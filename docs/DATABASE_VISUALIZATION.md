# 🗺️ Database Schema Visualization

## Entity Relationship Diagram (ERD)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          AUTHENTICATION & USERS                              │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────┐         ┌──────────────────┐         ┌──────────────────┐
│   auth.users     │────────▶│    profiles      │◀────────│  login_attempts  │
│  (Supabase)      │         │  - id (PK)       │         │  - email         │
│  - id            │         │  - email         │         │  - success       │
│  - email         │         │  - role          │         │  - ip_address    │
└──────────────────┘         │  - phone         │         └──────────────────┘
                             │  - verified      │
                             └────────┬─────────┘
                                      │
                      ┌───────────────┼───────────────┐
                      │               │               │
              ┌───────▼─────┐ ┌──────▼──────┐ ┌─────▼─────────┐
              │user_sessions│ │otp_verific  │ │activity_logs  │
              │- session_tk │ │ations       │ │- action       │
              │- user_id    │ │- phone      │ │- entity_type  │
              └─────────────┘ │- otp_code   │ └───────────────┘
                              └─────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                          ROOM MANAGEMENT                                     │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────┐
│     floors      │
│  - id (PK)      │
│  - floor_number │──┐
│  - name         │  │
└─────────────────┘  │
                     │
                     │      ┌──────────────────────┐
                     │      │    room_types        │
                     │      │  - id (PK)           │
                     │      │  - name              │──┐
                     │      │  - base_price        │  │
                     │      │  - max_occupancy     │  │
                     │      │  - amenities (JSONB) │  │
                     │      └──────────┬───────────┘  │
                     │                 │              │
                     │                 │              │
              ┌──────▼──────────┐      │       ┌──────▼────────────────┐
              │     rooms       │      │       │room_type_availability │
              │  - id (PK)      │      │       │  - room_type_id       │
              │  - room_number  │      │       │  - start_date         │
              │  - floor_id (FK)│      │       │  - end_date           │
              │  - room_type_id │◀─────┘       │  - is_available       │
              │  - status       │              │  - price_override     │
              └────────┬────────┘              └───────────────────────┘
                       │
                       │
┌─────────────────────────────────────────────────────────────────────────────┐
│                          BOOKINGS & PAYMENTS                                 │
└─────────────────────────────────────────────────────────────────────────────┘

            ┌──────────┴──────────┐
            │                     │
    ┌───────▼─────────────────────▼──────┐
    │         bookings                   │
    │  - id (PK)                         │
    │  - booking_reference               │
    │  - user_id (FK) ──────────────────┐│
    │  - room_id (FK)                   ││
    │  - room_type_id (FK)              ││
    │  - check_in_date                  ││
    │  - check_out_date                 ││
    │  - status (enum)                  ││
    │  - payment_status                 ││
    │  - total_price                    ││
    │  - checked_in_by (FK)             ││
    │  - checked_out_by (FK)            ││
    │  - reviewed_by (FK)               ││
    └──────┬──────────────┬─────────────┘│
           │              │              │
           │              │              └─────────┐
           │              │                        │
   ┌───────▼────────┐ ┌──▼───────────────┐ ┌──────▼──────────────┐
   │booking_services│ │payment_transact  │ │   stay_history      │
   │- booking_id    │ │ions              │ │ - booking_id        │
   │- service_id    │ │- booking_id      │ │ - user_id           │
   │- quantity      │ │- amount          │ │ - room_id           │
   │- unit_price    │ │- payment_method  │ │ - check_in_date     │
   │- service_date  │ │- gateway_name    │ │ - check_out_date    │
   └────────┬───────┘ │- received_by     │ │ - rating            │
            │         └──────────────────┘ │ - review            │
            │                              └─────────────────────┘
            │
┌─────────────────────────────────────────────────────────────────────────────┐
│                          SERVICES                                            │
└─────────────────────────────────────────────────────────────────────────────┘

            │
    ┌───────▼────────┐
    │   services     │
    │  - id (PK)     │
    │  - name        │
    │  - slug        │
    │  - category    │
    │  - price       │
    │  - is_available│
    └────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                          ANALYTICS & REPORTING                               │
└─────────────────────────────────────────────────────────────────────────────┘

┌────────────────────────┐
│room_occupancy_stats    │
│  - stat_date           │
│  - total_rooms         │
│  - occupied_rooms      │
│  - occupancy_rate      │
│  - revenue             │
└────────────────────────┘
```

---

## Database Flow Diagrams

### 1️⃣ User Registration & Authentication Flow

```
┌──────────┐
│  Client  │
└─────┬────┘
      │ 1. Sign up
      ▼
┌──────────────┐      2. Insert user      ┌─────────────┐
│ Supabase     │────────────────────────▶│ auth.users  │
│ Auth API     │                         └──────┬──────┘
└──────────────┘                                │
                                    3. Trigger: create_profile_on_signup()
                                                │
                                                ▼
                                         ┌──────────────┐
                                         │  profiles    │
                                         │ - role: user │
                                         └──────────────┘
```

### 2️⃣ Booking Creation Flow

```
┌──────────┐
│   User   │
└─────┬────┘
      │ 1. Select room & dates
      ▼
┌────────────────────────────────────┐
│ check_room_availability(room, dates)│
└──────────────┬─────────────────────┘
               │ 2. If available
               ▼
     ┌──────────────────┐
     │ INSERT bookings  │──┐ 3. Trigger: set_booking_reference()
     │ - status: pending│  │    Generates: BK-2025-12345
     │ - payment: pending│ │
     └──────────────────┘  │
                           ▼
                 ┌──────────────────────┐
                 │ UPDATE rooms         │
                 │ - status: reserved   │
                 └──────────────────────┘
```

### 3️⃣ Check-in Flow (Staff/Admin)

```
┌─────────┐
│  Staff  │
└────┬────┘
     │ 1. Scan booking reference
     ▼
┌────────────────────────────┐
│ SELECT bookings            │
│ WHERE reference = BK-XXX   │
└──────────┬─────────────────┘
           │ 2. Verify payment
           ▼
┌────────────────────────────┐
│ UPDATE bookings            │
│ - status: checked_in       │──┐ 3. Trigger: update_room_status_on_booking()
│ - actual_check_in_at: now()│  │
│ - checked_in_by: staff_id  │  │
└────────────────────────────┘  │
                                ▼
                      ┌──────────────────┐
                      │ UPDATE rooms     │
                      │ - status: occupied│
                      └──────────────────┘
```

### 4️⃣ Check-out Flow (Staff/Admin)

```
┌─────────┐
│  Staff  │
└────┬────┘
     │ 1. Process check-out
     ▼
┌────────────────────────────┐
│ UPDATE bookings            │
│ - status: checked_out      │──┐ 2. Trigger: create_stay_history_on_checkout()
│ - actual_check_out_at: now()│ │
│ - checked_out_by: staff_id │  │
└────────────────────────────┘  │
           │                    │
           │ 3. Trigger         ▼
           │          ┌──────────────────────┐
           │          │ INSERT stay_history  │
           │          │ - booking_id         │
           │          │ - user_id            │
           │          │ - dates              │
           │          │ - total_paid         │
           │          └──────────────────────┘
           ▼
┌──────────────────┐
│ UPDATE rooms     │
│ - status: cleaning│
└──────────────────┘
```

### 5️⃣ Payment Processing Flow

```
┌──────────┐
│   User   │
└─────┬────┘
      │ 1. Initiate payment
      ▼
┌─────────────────────┐
│ Payment Gateway     │
│ (Stripe/PayPal)     │
└──────────┬──────────┘
           │ 2. Process payment
           ▼
┌────────────────────────────────┐
│ INSERT payment_transactions    │──┐ 3. Trigger: set_transaction_reference()
│ - booking_id                   │  │    Generates: TXN-20251104123456-9876
│ - amount                       │  │
│ - payment_method: credit_card  │  │
│ - gateway_transaction_id       │  │
└────────────────────────────────┘  │
           │                        │
           │ 4. Update booking      │
           ▼                        │
┌────────────────────────────────┐  │
│ UPDATE bookings                │◀─┘
│ - payment_status: paid         │
│ - paid_amount                  │
└────────────────────────────────┘
```

---

## Access Control Matrix

| Resource | Public | User | Staff | Admin |
|----------|--------|------|-------|-------|
| **Profiles** |
| View own | ❌ | ✅ | ✅ | ✅ |
| View all | ❌ | ❌ | ✅ | ✅ |
| Update own | ❌ | ✅ | ✅ | ✅ |
| Update all | ❌ | ❌ | ❌ | ✅ |
| Change role | ❌ | ❌ | ❌ | ✅ |
| **Floors** |
| View | ✅ | ✅ | ✅ | ✅ |
| Create/Update/Delete | ❌ | ❌ | ❌ | ✅ |
| **Room Types** |
| View available | ✅ | ✅ | ✅ | ✅ |
| View all | ❌ | ❌ | ✅ | ✅ |
| Create/Update/Delete | ❌ | ❌ | ❌ | ✅ |
| **Rooms** |
| View active | ✅ | ✅ | ✅ | ✅ |
| Create/Delete | ❌ | ❌ | ❌ | ✅ |
| Update status | ❌ | ❌ | ✅ | ✅ |
| **Services** |
| View available | ✅ | ✅ | ✅ | ✅ |
| View all | ❌ | ❌ | ✅ | ✅ |
| Create/Update/Delete | ❌ | ❌ | ❌ | ✅ |
| **Bookings** |
| Create own | ❌ | ✅ | ✅ | ✅ |
| View own | ❌ | ✅ | ✅ | ✅ |
| View all | ❌ | ❌ | ✅ | ✅ |
| Update own (pending) | ❌ | ✅ | ✅ | ✅ |
| Update all | ❌ | ❌ | ✅ | ✅ |
| Cancel own | ❌ | ✅ | ✅ | ✅ |
| Delete | ❌ | ❌ | ❌ | ✅ |
| Check-in/Check-out | ❌ | ❌ | ✅ | ✅ |
| Review | ❌ | ❌ | ✅ | ✅ |
| **Payments** |
| Create own | ❌ | ✅ | ✅ | ✅ |
| View own | ❌ | ✅ | ✅ | ✅ |
| View all | ❌ | ❌ | ✅ | ✅ |
| Process | ❌ | ❌ | ✅ | ✅ |
| **Stay History** |
| View own | ❌ | ✅ | ✅ | ✅ |
| View all | ❌ | ❌ | ✅ | ✅ |
| Add review | ❌ | ✅ | ✅ | ✅ |
| **Activity Logs** |
| View all | ❌ | ❌ | ❌ | ✅ |
| View own | ❌ | ❌ | ✅ | ✅ |
| **Dashboard** |
| Access | ❌ | ❌ | ✅ | ✅ |

---

## Data Flow Summary

### Create → Read → Update → Delete (CRUD) Patterns

#### 1. Admin CRUD Operations
```
Admin → Room Type → Create/Update/Delete → Database
Admin → Room → Create/Update/Delete → Database
Admin → Floor → Create/Update/Delete → Database
Admin → Service → Create/Update/Delete → Database
```

#### 2. User Booking Journey
```
User → Browse Rooms → Check Availability → Create Booking
     → Add Services → Make Payment → Confirm Booking
     → Check-in (Staff) → Stay → Check-out (Staff)
     → Stay History → Leave Review
```

#### 3. Staff Operations
```
Staff → View Bookings → Review → Confirm/Reject
      → Check-in Guest → Update Room Status
      → Process Additional Charges
      → Check-out Guest → Create Stay History
      → View Dashboard
```

#### 4. Admin Analytics
```
Admin → Dashboard → View Statistics
      → Occupancy Rates → Revenue Reports
      → Popular Rooms → Popular Services
      → Activity Logs → User Management
```

---

## Performance Considerations

### Indexed Columns
- ✅ All primary keys (UUID)
- ✅ All foreign keys
- ✅ `booking_reference`, `transaction_reference`
- ✅ `email`, `phone` in profiles
- ✅ `status` in rooms and bookings
- ✅ `check_in_date`, `check_out_date`
- ✅ Date ranges for availability checks
- ✅ JSONB fields (amenities, features)
- ✅ Array fields (tags, images)

### Query Optimization
- **Views** for complex dashboard queries
- **Functions** for reusable logic
- **Triggers** for automatic updates
- **Constraints** for data validation

---

## Security Layers

```
┌─────────────────────────────────────────┐
│     1. Network Security (HTTPS)         │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│     2. Authentication (JWT)             │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│     3. Row Level Security (RLS)         │
│        - Role-based policies            │
│        - User-specific data access      │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│     4. Application Logic                │
│        - Input validation               │
│        - Business rules                 │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│     5. Database Constraints             │
│        - Foreign keys                   │
│        - Check constraints              │
│        - Unique constraints             │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│     6. Audit Logging                    │
│        - Activity logs                  │
│        - Login attempts                 │
│        - Session tracking               │
└─────────────────────────────────────────┘
```

---

**Last Updated:** November 4, 2025  
**Schema Version:** 1.0.0
